#include<stdio.h>
#include<string.h>
int main()
{
	int i,j,k;
	char a[20],b;
	gets(a);
	for(i=0;a[i]!='\0';i++);
	get(b);
	scanf("%d",k);
	for(j=0;j<=i;j++)
	{
		if(j>=k)
		{
			if(j==k)
			{
				
			}
		}
	}
	
}
